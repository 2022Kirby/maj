Title: Contact

tabs:
  mails:
    label: Mails de contact
    type: fields
    fields:
      autre:
          label: Autre
          type: email
          disabled: true
      maj:
        label: MAJ
        type: email
        required: true
      dev:
        label: Développeur(s)
        type: email
        required: true
        help: "Ces adresses mails sont utilisées pour le formulaire de contact."
      # dev:
      #   label: Mail(s) développeur(s)
      #   type: structure
      #   fields:
      #     adresse:
      #       label: Adresse
      #       type: email
      #   required: true
      #   help: "Ces adresses mails sont utilisées pour le formulaire de contact"
  formulaire:
    label: Messages formulaire
    type: fields
    fields:
      message:
        label: Mail à l'utilisateur
        type: textarea
        size: small
        required: true
        help: Ce message apparaîtra dans le mail de confirmation envoyé à l'utilisateur suite à l'envoi du formulaire.
      confirmation:
        label: Confirmation
        type: textarea
        size: small
        required: true
        help: Ce message apparaîtra sur la page de confirmation d'envoi du formulaire.
      echec:
        label: Échec
        type: textarea
        size: small
        required: true
        help: Ce message apparaîtra sur la page d'échec d'envoi du formulaire.